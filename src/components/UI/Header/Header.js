import React from 'react';
import classes from './Header.module.css'

const Header = () => {
    return (
        <header>
            <a href="/">Games App</a>
        </header>
    );
};

export default Header;
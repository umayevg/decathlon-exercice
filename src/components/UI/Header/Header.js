import React from 'react';
import classes from './Header.module.css'

const Header = () => {
    return (
        <header>
            <a href="/">Games DB</a>
        </header>
    );
};

export default Header;